name: Deploy KPI Tracker (web) to GitHub Pages

on:
  push:
    branches:
      - claude/kpi-tracker-mobile-app-a4lud7
    paths:
      - "kpi_tracker_app/**"
      - ".github/workflows/deploy-kpi-tracker-web.yml"
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        working-directory: kpi_tracker_app
        run: flutter pub get

      - name: Build web
        working-directory: kpi_tracker_app
        run: flutter build web --release --base-href /${{ github.event.repository.name }}/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: kpi_tracker_app/build/web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
